import Container from "@/components/ui/Container";

// 홈 화면 본문은 다음 단계에서 만든다. 여기서는 공용 껍데기가 붙는지만 확인한다.
export default function Home() {
  return (
    <Container>
      <div className="py-20">
        <h1>건강기능식품 B2B OEM/ODM 매칭</h1>
        <p className="pf-help mt-4">홈 화면은 다음 단계에서 구현합니다.</p>
      </div>
    </Container>
  );
}
